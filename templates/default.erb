# /etc/default/mesos

RUN_MESOS_SLAVE=no
RUN_MESOS_MASTER=no

MESOS_MASTER_PID="/var/run/mesos_master.pid"
MESOS_SLAVE_PID="/var/run/mesos_slave.pid"

MASTER_PORT=5050
MASTER_IP="<%= scope.lookupvar('mesos::params::mesos_master') %>"
LOG_DIR="/var/log/mesos"
MESOS_CONF="/etc/mesos/conf"

CLUSTER=""

