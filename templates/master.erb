#ZK=zk://localhost:2181/mesos
CLUSTER="<%= scope.lookupvar('mesos::params::cluster') %>"
IP="<%= scope.lookupvar('mesos::params::master') %>"
PORT=<%= scope.lookupvar('mesos::params::master_port') %>
SLAVES=*
#WHITELIST=*
#OPTS="--hadoop_home=/usr/lib/hadoop-0.20/"

# alternative is to use MESOS_<option> as an envar
# option list is available with /usr/sbin/mesos-master --help
# EX :
# export MESOS_log_dir=/var/log/mesos